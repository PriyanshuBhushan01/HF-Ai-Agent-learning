"""
Topic: [What is an Agent]


# Agent: An Ai agent is a system that laverage an ai model to interact with users, It actually does things like:
         
        ~ Reasoning
        ~ planning
        ~ execution of action

# Reasoning: Reasoning is how ai thinks through a goal before doing it.
            ~ Creating a roadmap of provided goal

# Planning: Planning is how ai breakes a goal into small steps.
           ~ Breaking a goal into small steps sequence 

# Action: Action is when agent is actually perform a task by using tools. 
          ~ Execute action by using multiple Api tools like api call, search, email, wheather,query, etc..   



 # Ai Agent example:

 ~ Reasoning: “User wants pizza → need restaurants → need menu → need payment.”

 ~ Planning: “Step 1: find restaurants → Step 2: compare → Step 3: order.”

 ~ Action: Calls APIs → places order → sends confirmation.


 # API tools used in this example:

 ※ Calls a location API → gets your address

 ※ Calls a restaurant API → fetches pizza places

 ※ Calls a menu API → checks items

 ※ Calls a payment API → pays
"""




